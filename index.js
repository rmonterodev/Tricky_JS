// Import stylesheets
import './style.css';

// Write Javascript code!
const appDiv = document.getElementById('app');
appDiv.innerHTML = `<h1>JS tricky questions</h1>`;

//concantened. + means used for strings when dont have at least two numbers. Converted the other value into a string.
console.log(2 + '2') 

console.log(2 - '2') 

